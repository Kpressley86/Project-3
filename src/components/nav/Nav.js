import React, { Component } from 'react';
import Login from '../Login';

class Nav extends Component {
    render() {
        return(
          <nav>
             <Login />  
          </nav>
             
        );
    }
}

export default Nav;